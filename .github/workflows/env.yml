name: env
on: push
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Get codes
        uses: actions/checkout@v4

      - name: Install Deps
        run: npm install

      - name: build
        run: npm run build

      - name: test
        run: npm run test